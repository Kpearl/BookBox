<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CommonMapper">
<!-- 
	<resultMap id="gradeSelectMap" type="grade">
		<result property="average" column="grade" jdbcType="INTEGER" />
		<result property="userCount" column="tag_no" jdbcType="INTEGER" />
		<result property="doGrade" column="tag_no" jdbcType="INTEGER" />
	</resultMap>

	<resultMap id="likeSelectMap" type="like">
		<result property="totalLike" column="tag_name" jdbcType="VARCHAR" />
		<result property="doLike" column="tag_no" jdbcType="INTEGER" />
	</resultMap>

	<resultMap id="replySelectMap" type="reply">
		<result property="email" column="email" jdbcType="VARCHAR" />
		<result property="content" column="reply_content" jdbcType="VARCHAR" />
		<result property="content" column="reply_content" jdbcType="VARCHAR" />
	</resultMap>
 -->
	<!-- <insert id="addBookLike" parameterType="tag" useGeneratedKeys="true" 
		keyProperty="tagNo"> INSERT INTO tag( tag_name ) VALUES ( #{tagName} ) </insert> 
		<delete id="deleteBookLike" parameterType="map"> DELETE FROM tag_group WHERE 
		category_no = #{categoryNo} AND target_no = #{targetNo} </delete> -->
	<insert id="addBookReply" parameterType="map" useGeneratedKeys="true" keyProperty="reply_no">
		INSERT
		INTO reply(email, category_no, target_no, reply_content)
		VALUES ( #{user.email}, #{category}, #{isbn}, #{reply.content})
	</insert>
	<!-- <delete id="deleteBookReply" parameterType="map"> DELETE FROM tag_group 
		WHERE category_no = #{categoryNo} AND target_no = #{targetNo} </delete> -->
	<!-- <insert id="addBookGrade" parameterType="tag" useGeneratedKeys="true" 
		keyProperty="tagNo"> INSERT INTO tag( tag_name ) VALUES ( #{tagName} ) </insert> -->
</mapper>